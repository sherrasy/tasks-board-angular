import { HideForUnauthorized } from './hide-for-unauthorized';

describe('HideForUnauthorized', () => {
  it('should create an instance', () => {
    const directive = new HideForUnauthorized();
    expect(directive).toBeTruthy();
  });
});

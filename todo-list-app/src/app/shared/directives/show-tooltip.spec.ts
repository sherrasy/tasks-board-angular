import { ShowTooltip } from './show-tooltip';

describe('ShowTooltip', () => {
  it('should create an instance', () => {
    const directive = new ShowTooltip();
    expect(directive).toBeTruthy();
  });
});
